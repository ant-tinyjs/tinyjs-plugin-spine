/**
 * Determines how images are blended with existing pixels when drawn.
 *
 * @readonly
 * @enum {number}
 * @memberof Tiny.spine
 */
const BlendMode = {
  Normal: 0,
  Additive: 1,
  Multiply: 2,
  Screen: 3,
};

export default BlendMode;
